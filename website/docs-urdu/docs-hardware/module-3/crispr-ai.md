---
sidebar_position: 3
---

# AI کے ساتھ CRISPR ٹارگٹ پیش گوئی
## تعارف
CRISPR-Cas9 جین ایڈیٹنگ ٹول نے پودوں کی بائیوٹیکنالوجی کے فیلڈ کو تبدیل کر دیا ہے، پودوں کے جینومز میں درست ترمیموں کو ممکن بنا کر۔ تاہم، CRISPR-Cas9 کی کارکردگی اور مخصوصیت گائیڈ RNA (gRNA) کے ڈیزائن پر منحصر ہے۔ اس ماڈیول میں، ہم دریافت کریں گے کہ artificial intelligence (AI) gRNA کارکردگی کی پیش گوئی، آف ٹارگٹ اثرات، اور فصل کی بہتری کے لیے بہترین ایڈیٹنگ حکمت عملیوں کو ڈیزائن کر کے CRISPR جین ایڈیٹنگ کو کیسے بہتر بنا سکتی ہے۔ 🌱

## بنیادی تصورات
### CRISPR-Cas9 بنیادی باتیں اور جین ایڈیٹنگ
CRISPR-Cas9 ایک بیکٹیریل ڈیفنس سسٹم ہے جو جین ایڈیٹنگ کے لیے دوبارہ استعمال کیا گیا ہے۔ اس میں دو اہم اجزاء شامل ہیں: Cas9 اینزائم اور gRNA۔ gRNA کو DNA کی ایک مخصوص سیکوینس سے بائنڈ کرنے کے لیے ڈیزائن کیا جاتا ہے، اور Cas9 اینزائم اس جگہ پر DNA کو کاٹتا ہے۔ یہ ڈبل اسٹرینڈڈ بریک بناتا ہے، جو سیل کی قدرتی مرمت مشینری کے ذریعے مرمت کیا جا سکتا ہے۔ مطلوبہ ایڈٹ کے ساتھ ٹیمپلیٹ متعارف کروا کر، سیل جینوم میں ایڈٹ کو شامل کر سکتا ہے۔

### گائیڈ RNA ڈیزائن چیلنجز
موثر gRNAs ڈیزائن کرنا کامیاب CRISPR-Cas9 جین ایڈیٹنگ کے لیے ضروری ہے۔ gRNA کو ٹارگٹ سیکوینس کے لیے مخصوص ہونا چاہیے، اور اس کی کارکردگی آف ٹارگٹ سائٹس کی موجودگی، gRNA کی سیکنڈری سٹرکچر، اور ٹارگٹ سائٹ کی رسائی جیسے مختلف عوامل سے متاثر ہو سکتی ہے۔

### آن ٹارگٹ ایکٹیویٹی پیش گوئی کے لیے ML ماڈلز
Machine learning (ML) ماڈلز gRNAs کی آن ٹارگٹ ایکٹیویٹی کی پیش گوئی کے لیے استعمال کیے جا سکتے ہیں۔ یہ ماڈلز gRNA سیکوینسز اور ان کی متعلقہ ایکٹیویٹیز کے بڑے ڈیٹاسیٹس پر ٹرین کیے جا سکتے ہیں۔ مثال کے طور پر، درج ذیل Python کوڈ gRNA سیکوینسز اور ان کی ایکٹیویٹیز کے ڈیٹاسیٹ پر رینڈم فارسٹ ماڈل کو ٹرین کرنے کے لیے scikit-learn کا استعمال کرتا ہے:

```python
import pandas as pd
from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split

# ڈیٹاسیٹ لوڈ کریں
df = pd.read_csv('gRNA_dataset.csv')

# ڈیٹاسیٹ کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں
X_train, X_test, y_train, y_test = train_test_split(df['gRNA_sequence'], df['activity'], test_size=0.2, random_state=42)

# رینڈم فارسٹ ماڈل ٹرین کریں
model = RandomForestRegressor(n_estimators=100, random_state=42)
model.fit(X_train, y_train)

# ماڈل کی تشخیص کریں
y_pred = model.predict(X_test)
print('R-squared:', model.score(X_test, y_test))
```

### آف ٹارگٹ اثرات کی پیش گوئی اور کم سے کم کرنا
آف ٹارگٹ اثرات اس وقت ہوتے ہیں جب gRNA جینوم میں غیر ارادی سائٹس سے بائنڈ ہوتا ہے، جس سے غیر مطلوبہ ایڈٹس ہوتے ہیں۔ ML ماڈلز آف ٹارگٹ اثرات کی پیش گوئی کے لیے بھی استعمال کیے جا سکتے ہیں۔ مثال کے طور پر، درج ذیل Python کوڈ gRNA سیکوینسز اور ان کے متعلقہ آف ٹارگٹ اثرات کے ڈیٹاسیٹ پر convolutional neural network (CNN) کو ٹرین کرنے کے لیے TensorFlow کا استعمال کرتا ہے:

```python
import numpy as np
import tensorflow as tf
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Conv1D, MaxPooling1D, Flatten, Dense

# ڈیٹاسیٹ لوڈ کریں
df = pd.read_csv('off_target_dataset.csv')

# ڈیٹاسیٹ کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں
X_train, X_test, y_train, y_test = train_test_split(df['gRNA_sequence'], df['off_target_effect'], test_size=0.2, random_state=42)

# gRNA سیکوینسز کو عددی نمائندگیوں میں تبدیل کریں
X_train_num = np.array([np.array([ord(c) for c in seq]) for seq in X_train])
X_test_num = np.array([np.array([ord(c) for c in seq]) for seq in X_test])

# CNN ماڈل ٹرین کریں
model = Sequential()
model.add(Conv1D(32, kernel_size=3, activation='relu', input_shape=(len(X_train_num[0]), 1)))
model.add(MaxPooling1D(pool_size=2))
model.add(Flatten())
model.add(Dense(64, activation='relu'))
model.add(Dense(1))
model.compile(loss='mean_squared_error', optimizer='adam')
model.fit(X_train_num, y_train, epochs=10, batch_size=32, validation_data=(X_test_num, y_test))

# ماڈل کی تشخیص کریں
y_pred = model.predict(X_test_num)
print('R-squared:', model.evaluate(X_test_num, y_test))
```

### sgRNA کارکردگی اسکورنگ کے لیے ڈیپ لرننگ
ڈیپ لرننگ ماڈلز sgRNAs کی کارکردگی کو اسکور کرنے کے لیے استعمال کیے جا سکتے ہیں۔ مثال کے طور پر، درج ذیل Python کوڈ sgRNA سیکوینسز اور ان کی متعلقہ کارکردگیوں کے ڈیٹاسیٹ پر recurrent neural network (RNN) کو ٹرین کرنے کے لیے PyTorch کا استعمال کرتا ہے:

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import Dataset, DataLoader

# کسٹم ڈیٹاسیٹ کلاس کی تعریف کریں
class sgRNA_Dataset(Dataset):
    def __init__(self, sequences, efficiencies):
        self.sequences = sequences
        self.efficiencies = efficiencies

    def __len__(self):
        return len(self.sequences)

    def __getitem__(self, idx):
        sequence = self.sequences[idx]
        efficiency = self.efficiencies[idx]
        return {'sequence': sequence, 'efficiency': efficiency}

# ڈیٹاسیٹ لوڈ کریں
df = pd.read_csv('sgRNA_dataset.csv')

# ڈیٹاسیٹ کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں
X_train, X_test, y_train, y_test = train_test_split(df['sgRNA_sequence'], df['efficiency'], test_size=0.2, random_state=42)

# کسٹم ڈیٹاسیٹ انسٹینسز بنائیں
train_dataset = sgRNA_Dataset(X_train, y_train)
test_dataset = sgRNA_Dataset(X_test, y_test)

# ڈیٹا لوڈرز بنائیں
train_loader = DataLoader(train_dataset, batch_size=32, shuffle=True)
test_loader = DataLoader(test_dataset, batch_size=32, shuffle=False)

# RNN ماڈل کی تعریف کریں
class RNN_Model(nn.Module):
    def __init__(self):
        super(RNN_Model, self).__init__()
        self.rnn = nn.LSTM(input_size=4, hidden_size=64, num_layers=1, batch_first=True)
        self.fc = nn.Linear(64, 1)

    def forward(self, x):
        h0 = torch.zeros(1, x.size(0), 64).to(x.device)
        c0 = torch.zeros(1, x.size(0), 64).to(x.device)

        out, _ = self.rnn(x, (h0, c0))
        out = self.fc(out[:, -1, :])
        return out

# ماڈل، آپٹیمائزر، اور لاس فنکشن کو انیشیلائز کریں
model = RNN_Model()
optimizer = optim.Adam(model.parameters(), lr=0.001)
loss_fn = nn.MSELoss()

# ماڈل ٹرین کریں
for epoch in range(10):
    for batch in train_loader:
        sequences = batch['sequence']
        efficiencies = batch['efficiency']

        # سیکوینسز کو عددی نمائندگیوں میں تبدیل کریں
        sequences_num = torch.tensor([np.array([ord(c) for c in seq]) for seq in sequences])

        # گریدیئنٹس کو زیرو کریں
        optimizer.zero_grad()

        # فارورڈ پاس
        outputs = model(sequences_num)
        loss = loss_fn(outputs, efficiencies)

        # بیکوارڈ پاس
        loss.backward()

        # ماڈل پیرامیٹرز اپ ڈیٹ کریں
        optimizer.step()

    print('Epoch {}: Loss = {:.4f}'.format(epoch+1, loss.item()))

# ماڈل کی تشخیص کریں
model.eval()
with torch.no_grad():
    total_loss = 0
    for batch in test_loader:
        sequences = batch['sequence']
        efficiencies = batch['efficiency']

        # سیکوینسز کو عددی نمائندگیوں میں تبدیل کریں
        sequences_num = torch.tensor([np.array([ord(c) for c in seq]) for seq in sequences])

        # فارورڈ پاس
        outputs = model(sequences_num)
        loss = loss_fn(outputs, efficiencies)
        total_loss += loss.item()

    print('Test Loss: {:.4f}'.format(total_loss / len(test_loader)))
```

### ملٹی پلکسڈ ایڈیٹنگ حکمت عملی کی بہتری
ملٹی پلکسڈ ایڈیٹنگ میں ایک جینوم میں متعدد ایڈٹس ایک ساتھ کرنا شامل ہے۔ AI ملٹی پلکسڈ ایڈیٹنگ حکمت عملیوں کو متعدد gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کر کے بہتر بنا سکتی ہے۔ مثال کے طور پر، درج ذیل Python کوڈ ملٹی پلکسڈ ایڈیٹنگ حکمت عملی کو بہتر بنانے کے لیے جینیٹک الگورتھم کا استعمال کرتا ہے:

```python
import numpy as np
import random

# فٹنس فنکشن کی تعریف کریں
def fitness(gRNA_set):
    # ہر gRNA کی کارکردگی اور مخصوصیت کا حساب لگائیں
    efficiencies = []
    specificities = []
    for gRNA in gRNA_set:
        # ML ماڈلز کا استعمال کرتے ہوئے کارکردگی اور مخصوصیت کا حساب لگائیں
        efficiency = ml_model.predict(gRNA)
        specificity = ml_model.predict(gRNA)
        efficiencies.append(efficiency)
        specificities.append(specificity)

    # مجموعی فٹنس کا حساب لگائیں
    fitness = np.mean(efficiencies) * np.mean(specificities)
    return fitness

# جینیٹک الگورتھم کی تعریف کریں
def genetic_algorithm(population_size, num_generations):
    # آبادی کو انیشیلائز کریں
    population = []
    for _ in range(population_size):
        gRNA_set = random.sample(gRNA_library, num_gRNAs)
        population.append(gRNA_set)

    # آبادی کو ایوولو کریں
    for _ in range(num_generations):
        # ہر انفرادی کی فٹنس کا حساب لگائیں
        fitnesses = []
        for individual in population:
            fitnesses.append(fitness(individual))

        # سب سے فٹ انفرادیز کو سلیکٹ کریں
        fittest_individuals = np.argsort(fitnesses)[-int(population_size/2):]

        # کراس اوور اور میوٹ کریں
        new_population = []
        for _ in range(population_size):
            parent1 = random.choice(fittest_individuals)
            parent2 = random.choice(fittest_individuals)
            child = crossover(parent1, parent2)
            child = mutate(child)
            new_population.append(child)

        # کم سے کم فٹ انفرادیز کو تبدیل کریں
        population = new_population

    # سب سے فٹ انفرادی واپس کریں
    return population[0]

# کراس اوور اور میوٹیشن فنکشنز کی تعریف کریں
def crossover(parent1, parent2):
    # رینڈم پوائنٹ پر کراس اوور کریں
    crossover_point = random.randint(1, num_gRNAs-1)
    child = parent1[:crossover_point] + parent2[crossover_point:]
    return child

def mutate(individual):
    # رینڈم پوائنٹ پر میوٹ کریں
    mutation_point = random.randint(0, num_gRNAs-1)
    individual[mutation_point] = random.choice(gRNA_library)
    return individual

# جینیٹک الگورتھم چلائیں
gRNA_library = ['gRNA1', 'gRNA2', 'gRNA3', 'gRNA4', 'gRNA5']
num_gRNAs = 3
population_size = 100
num_generations = 100
fittest_individual = genetic_algorithm(population_size, num_generations)
print('سب سے فٹ انفرادی:', fittest_individual)
```

### AI ڈیزائنڈ فصل: مثالوں اور کیس اسٹڈیز
AI مطلوبہ ٹریٹس والی فصل ڈیزائن کرنے کے لیے استعمال کی جا سکتی ہے، جیسے ڈراوٹ ریزسٹنس یا بڑھا ہوا ییلڈ۔ مثال کے طور پر، ایک اسٹڈی نے ڈراوٹ ریلیٹڈ جینز کو ٹارگٹ کرنے والے gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کر کے ڈراوٹ ریزسٹنٹ گندم کی اقسام ڈیزائن کرنے کے لیے AI کا استعمال کیا۔ نتیجے میں گندم کی اقسام نے بہتر ڈراوٹ ریزسٹنس اور ییلڈ دکھایا۔

### عملی پروجیکٹ: گندم میں ڈراوٹ ریزسٹنس کے لیے بہترین CRISPR ایڈٹس ڈیزائن کریں
اس پروجیکٹ میں، آپ گندم میں ڈراوٹ ریزسٹنس کے لیے بہترین CRISPR ایڈٹس ڈیزائن کرنے کے لیے AI کا استعمال کریں گے۔ آپ ڈراوٹ ریلیٹڈ جینز کو ٹارگٹ کرنے والے gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کے لیے ML ماڈلز کا استعمال کریں گے، اور پھر ملٹی پلکسڈ ایڈیٹنگ حکمت عملی کو بہتر بنانے کے لیے جینیٹک الگورتھم کا استعمال کریں گے۔

**مرحلہ 1: ڈیٹاسیٹ لوڈ کریں اور ML ماڈلز ٹرین کریں**

* gRNA سیکوینسز اور ان کی متعلقہ ایکٹیویٹیز کے ڈیٹاسیٹ لوڈ کریں
* gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کے لیے ML ماڈلز ٹرین کریں

**مرحلہ 2: فٹنس فنکشن اور جینیٹک الگورتھم کی تعریف کریں**

* gRNAs کے سیٹ کی کارکردگی اور مخصوصیت کا حساب لگانے والا فٹنس فنکشن کی تعریف کریں
* آبادی میں gRNA سیٹس کو ایوولو کر کے فٹنس فنکشن کو بہتر بنانے والا جینیٹک الگورتھم کی تعریف کریں

**مرحلہ 3: جینیٹک الگورتھم چلائیں اور سب سے فٹ انفرادی سلیکٹ کریں**

* gRNA سیٹس کی آبادی کو ایوولو کرنے کے لیے جینیٹک الگورتھم چلائیں
* سب سے فٹ انفرادی سلیکٹ کریں اور نتیجے میں gRNA سیٹ پرنٹ کریں

**مرحلہ 4: نتائج کی توثیق کریں**

* منتخب gRNA سیٹ کی کارکردگی اور مخصوصیت کی پیش گوئی کر کے نتائج کی توثیق کریں
* AI ڈیزائنڈ CRISPR ایڈٹس کی تاثیر کا تعین کرنے کے لیے نتائج کو کنٹرول گروپ سے موازنہ کریں

### بہترین طریقے اور عام غلطیاں
CRISPR ایڈٹس ڈیزائن کرنے کے لیے AI استعمال کرتے وقت، درج ذیل بہترین طریقوں اور عام غلطیوں پر غور کرنا ضروری ہے:

* **ہائی کوالٹی ڈیٹاسیٹس استعمال کریں**: ML ماڈلز کو ٹرین کرنے کے لیے استعمال ہونے والے ڈیٹاسیٹ کی کوالٹی پیش گوئیاں کی درستگی کو نمایاں طور پر متاثر کر سکتی ہے۔
* **نتائج کی توثیق کریں**: AI ڈیزائنڈ CRISPR ایڈٹس کے نتائج کی توثیق کریں تاکہ ان کی تاثیر اور مخصوصیت یقینی بنائیں۔
* **آف ٹارگٹ اثرات پر غور کریں**: gRNAs کے ممکنہ آف ٹارگٹ اثرات پر غور کریں اور انہیں کم سے کم کرنے کے لیے حکمت عملیوں کا استعمال کریں۔
* **ملٹی پلکسڈ ایڈیٹنگ استعمال کریں**: ایک جینوم میں متعدد ایڈٹس ایک ساتھ کرنے کے لیے ملٹی پلکسڈ ایڈیٹنگ استعمال کریں، جو ایڈٹس کی کارکردگی اور مخصوصیت کو بہتر بنا سکتی ہے۔

### ہینڈز آن مثال یا مینی پروجیکٹ
اس ہینڈز آن مثال میں، آپ گندم میں ڈراوٹ ریزسٹنس کے لیے بہترین CRISPR ایڈٹس ڈیزائن کرنے کے لیے AI کا استعمال کریں گے۔ آپ ڈراوٹ ریلیٹڈ جینز کو ٹارگٹ کرنے والے gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کے لیے ML ماڈلز کا استعمال کریں گے، اور پھر ملٹی پلکسڈ ایڈیٹنگ حکمت عملی کو بہتر بنانے کے لیے جینیٹک الگورتھم کا استعمال کریں گے۔

### خلاصہ ٹیبل یا چیک لسٹ
اس ماڈیول میں کور کیپٹس اور تکنیکوں کا خلاصہ ٹیبل یہاں ہے:

| تصور | تفصیل |
| --- | --- |
| CRISPR-Cas9 | جین ایڈیٹنگ ٹول جو DNA کی مخصوص سیکوینس سے بائنڈ کرنے کے لیے gRNA کا استعمال کرتا ہے اور اس جگہ پر DNA کو کاٹتا ہے |
| گائیڈ RNA ڈیزائن | ٹارگٹ سیکوینس کے لیے مخصوص اور ہائی کارکردگی والے gRNAs ڈیزائن کرنا |
| ML ماڈلز | gRNAs کی کارکردگی اور مخصوصیت کی پیش گوئی کے لیے استعمال ہونے والے مشین لرننگ ماڈلز |
| آف ٹارگٹ اثرات | غیر مطلوبہ ایڈٹس جو اس وقت ہوتے ہیں جب gRNA جینوم میں غیر ارادی سائٹس سے بائنڈ ہوتا ہے |
| ملٹی پلکسڈ ایڈیٹنگ | متعدد gRNAs کا استعمال کرتے ہوئے ایک جینوم میں متعدد ایڈٹس ایک ساتھ کرنا |
| جینیٹک الگورتھم | آبادی میں سب سے فٹ انفرادی تلاش کرنے کے لیے ایوولوشن کا استعمال کرنے والی بہتری تکنیک |
| AI ڈیزائنڈ فصل | فصل جو ڈراوٹ ریزسٹنس یا بڑھے ہوئے ییلڈ جیسے مطلوبہ ٹریٹس کے لیے AI کا استعمال کرتے ہوئے ڈیزائن کی گئی ہیں |

### اگلے اقدامات اور مزید مطالعہ
اگلے ماڈیول میں، آپ پودوں کی بریڈنگ اور جینٹکس میں AI کے اطلاقات سیکھیں گے۔ آپ دریافت کریں گے کہ AI فصل کی کارکردگی کی پیش گوئی، مطلوبہ ٹریٹس سے وابستہ جینیاتی ویریئنٹس کی شناخت، اور فصل کی پیداوار کو بہتر بنانے کے لیے بریڈنگ پروگرامز ڈیزائن کرنے کے لیے کیسے استعمال کی جا سکتی ہے۔

مزید مطالعہ کے لیے، آپ درج ذیل ریسورسز کا حوالہ دے سکتے ہیں:

* **CRISPR-Cas9: جینوم ایڈیٹنگ کے لیے ایک طاقتور ٹول** by Jennifer Doudna and Emmanuelle Charpentier
* **CRISPR-Cas9 جین ایڈیٹنگ کے لیے مشین لرننگ** by David Liu and colleagues
* **AI ڈیزائنڈ فصل: پودوں کی بریڈنگ میں ایک نیا دور** by Pamela Ronald and colleagues

میں امید کرتا ہوں کہ اس ماڈیول نے آپ کو فصل کی بہتری کے لیے CRISPR جین ایڈیٹنگ کو بہتر بنانے کے لیے AI کے استعمال کی جامع سمجھ فراہم کی ہے۔ یاد رکھیں کہ اس ماڈیول میں کور کیپٹس اور تکنیکوں پر عمل کریں، اور اگر آپ کے پاس کوئی سوالات ہیں یا مزید وضاحت کی ضرورت ہے تو ہچکچاہٹ نہ کریں۔ 💡