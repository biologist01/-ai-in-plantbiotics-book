---
sidebar_position: 5
---
# ماڈیول 3: مینی پروجیکٹ - ٹریٹ پیش گوئی سسٹم
## تعارف
پودوں کی بائیوٹیکنالوجی میں artificial intelligence (AI) اور machine learning (ML) کے اطلاق نے جینوٹائپک ڈیٹا سے پودوں کے فینوٹائپس کی پیش گوئی کو ممکن بنا کر فیلڈ کو تبدیل کر دیا ہے۔ اس کا فصل کی بہتری اور بریڈنگ پروگرامز کے لیے نمایاں اثرات ہیں۔ اس ماڈیول میں، ہم جینومک ٹو فینوٹائپک پیش گوئی پائپ لائن کے تصور کا جائزہ لیں گے اور ensemble ML طریقوں کا استعمال کرتے ہوئے ٹریٹ پیش گوئی سسٹم بنائیں گے۔ ہم چاول اور مکئی سے حقیقی ڈیٹاسیٹس کا استعمال کرتے ہوئے اپنی پیش گوئیاں کی توثیق کریں گے۔

جینوٹائپک ڈیٹا سے پودوں کے فینوٹائپس کی پیش گوئی کرنے کی صلاحیت پودوں کے بریڈرز کو مطلوبہ ٹریٹس کی شناخت کرنے اور فصل کی سلیکشن اور بریڈنگ کے بارے میں آگاہ فیصلے کرنے میں مدد کر سکتی ہے۔ مثال کے طور پر، گندم میں ڈراوٹ ٹولرنس کی پیش گوئی 🌾 یا ٹماٹر میں بیماری کی مزاحمت 🍅 بریڈرز کو زیادہ لچکدار اور پیداوار والے پودوں کی ترقی میں مدد کر سکتی ہے۔

## بنیادی تصورات
پروجیکٹ میں غوطہ لگانے سے پہلے، آئیے کچھ بنیادی تصورات کا احاطہ کریں:

* **جینومک ٹو فینوٹائپک پیش گوئی پائپ لائن**: یہ جینومک ڈیٹا (جیسے، DNA سیکوینسز) کا استعمال کرتے ہوئے فینوٹائپک ٹریٹس (جیسے، پودوں کی اونچائی، پیداوار) کی پیش گوئی کرنے کے عمل کا حوالہ دیتا ہے۔
* **سنگل نیوکلیوٹائڈ پولیمورفزم (SNPs)**: یہ جینوم میں ایک مخصوص پوزیشن پر ہونے والی سنگل نیوکلیوٹائڈ میں تغیرات ہیں۔ SNPs کو فینوٹائپک ٹریٹس کی پیش گوئی کے لیے مارکرز کے طور پر استعمال کیا جا سکتا ہے۔
* **Ensemble طریقے**: یہ ML تکنیکیں ہیں جو زیادہ درست پیش گوئی پیدا کرنے کے لیے متعدد ماڈلز کی پیش گوئیاں کو جوڑتی ہیں۔
* **کراس ویلیڈیشن**: یہ ایک تکنیک ہے جو ڈیٹا کے متعدد سب سیٹس پر ماڈل کو ٹرین اور ٹیسٹ کر کے ماڈل کی کارکردگی کی تشخیص کے لیے استعمال کی جاتی ہے۔

### SNP فلٹرنگ اور کوالٹی کنٹرول
SNP فلٹرنگ اور کوالٹی کنٹرول جینومک ٹو فینوٹائپک پیش گوئی پائپ لائن میں اہم اقدامات ہیں۔ ہمیں یہ یقینی بنانے کی ضرورت ہے کہ ہم جو SNPs استعمال کرتے ہیں وہ اعلی کوالٹی کے ہیں اور ہم جس ٹریٹ کی پیش گوئی کرنے کی کوشش کر رہے ہیں اس سے متعلق ہیں۔

```python
import pandas as pd
import numpy as np

# SNP ڈیٹا لوڈ کریں
snp_data = pd.read_csv('snp_data.csv')

# کم کوالٹی سکور والے SNPs کو فلٹر کریں
snp_data = snp_data[snp_data['quality_score'] > 0.5]

# ڈپلیکیٹ SNPs کو ہٹائیں
snp_data = snp_data.drop_duplicates()

print(snp_data.head())
```

### جینومک مارکرز سے فیچر انجینئرنگ
ہم جینومک مارکرز سے فیچرز انجینئر کرنے کے لیے مختلف تکنیکوں کا استعمال کر سکتے ہیں، جیسے:

* **SNP انکوڈنگ**: یہ SNPs کو عددی ویلیوز میں تبدیل کرنے شامل ہے جو ML ماڈلز کے لیے ان پٹ فیچرز کے طور پر استعمال کیے جا سکتے ہیں۔
* **جینومک ریلیشن شپ میٹرکس**: یہ ایک میٹرکس ہے جو انفرادیوں کے درمیان جینیاتی ریلیشن شپس کو ان کے جینومک ڈیٹا کی بنیاد پر نمائندگی کرتا ہے۔

```python
import numpy as np

# SNP انکوڈ کرنے کے لیے فنکشن کی تعریف کریں
def encode_snp(snp):
    if snp == 'A':
        return 0
    elif snp == 'C':
        return 1
    elif snp == 'G':
        return 2
    elif snp == 'T':
        return 3

# SNP ڈیٹا پر انکوڈنگ فنکشن لاگو کریں
snp_data['encoded_snp'] = snp_data['snp'].apply(encode_snp)

print(snp_data.head())
```

### ٹریٹ پیش گوئی کے لیے Ensemble طریقے
ہم جینومک ڈیٹا سے ٹریٹس کی پیش گوئی کے لیے رینڈم فارسٹس یا گریدیئنٹ بوسٹنگ جیسے ensemble طریقوں کا استعمال کر سکتے ہیں۔

```python
from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split

# ڈیٹا کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں
X_train, X_test, y_train, y_test = train_test_split(snp_data, phenotypic_data, test_size=0.2, random_state=42)

# رینڈم فارسٹ ماڈل ٹرین کریں
rf_model = RandomForestRegressor(n_estimators=100, random_state=42)
rf_model.fit(X_train, y_train)

# ٹیسٹنگ سیٹ پر پیش گوئیاں کریں
y_pred = rf_model.predict(X_test)

print(y_pred)
```

### مختلف انوائرنمنٹس کے ساتھ کراس ویلیڈیشن
ہم مختلف انوائرنمنٹس میں اپنے ماڈل کی کارکردگی کی تشخیص کے لیے کراس ویلیڈیشن کا استعمال کر سکتے ہیں۔

```python
from sklearn.model_selection import KFold

# کراس ویلیڈیشن کرنے کے لیے فنکشن کی تعریف کریں
def cross_validate(model, X, y):
    kf = KFold(n_splits=5, shuffle=True, random_state=42)
    scores = []
    for train_index, test_index in kf.split(X):
        X_train, X_test = X[train_index], X[test_index]
        y_train, y_test = y[train_index], y[test_index]
        model.fit(X_train, y_train)
        y_pred = model.predict(X_test)
        score = model.score(X_test, y_test)
        scores.append(score)
    return np.mean(scores)

# کراس ویلیڈیشن کریں
score = cross_validate(rf_model, snp_data, phenotypic_data)
print(score)
```

### ماڈل تشریح: کیوزل ویریئنٹس کی شناخت
ہم پیش گوئیاں چلا رہے کیوزل ویریئنٹس کی شناخت کے لیے پرمیوٹیشن اہمیت یا SHAP ویلیوز جیسے تکنیکوں کا استعمال کر سکتے ہیں۔

```python
import shap

# SHAP ایکسپلائنر بنائیں
explainer = shap.Explainer(rf_model)

# ٹیسٹنگ سیٹ کے لیے SHAP ویلیوز حاصل کریں
shap_values = explainer(X_test)

# SHAP ویلیوز پلٹ کریں
shap.plots.beeswarm(shap_values)
```

### بریڈنگ پروگرامز کے ساتھ انٹیگریشن
ٹریٹ پیش گوئی سسٹم کو بریڈنگ پروگرامز کے ساتھ انٹیگریٹ کیا جا سکتا ہے تاکہ مطلوبہ ٹریٹس کی شناخت کی جا سکے اور فصل کی سلیکشن اور بریڈنگ کے بارے میں آگاہ فیصلے کیے جا سکیں۔

| ٹریٹ | پیش گوئی | بریڈنگ فیصلہ |
| --- | --- | --- |
| ڈراوٹ ٹولرنس | ہائی | بریڈنگ کے لیے سلیکٹ کریں |
| بیماری کی مزاحمت | لو | بریڈنگ کے لیے ایوائڈ کریں |
| پیداوار | میڈیم | بریڈنگ کے لیے کونسڈر کریں |

## زراعت/پودوں کی سائنس میں عملی اطلاقات
ٹریٹ پیش گوئی سسٹم میں زراعت اور پودوں کی سائنس میں بہت سے عملی اطلاقات ہیں، بشمول:

* **فصل کی بہتری**: سسٹم کو مطلوبہ ٹریٹس کی شناخت اور فصل کی سلیکشن اور بریڈنگ کے بارے میں آگاہ فیصلے کرنے کے لیے استعمال کیا جا سکتا ہے۔
* **پریسیژن زراعت**: سسٹم کو مختلف انوائرنمنٹس میں پودوں کی کارکردگی کی پیش گوئی اور فصل مینجمنٹ پریکٹسز کو بہتر بنانے کے لیے استعمال کیا جا سکتا ہے۔
* **پودوں کی بریڈنگ**: سسٹم کو کیوزل ویریئنٹس کی شناخت اور بریڈنگ پروگرامز کے بارے میں آگاہ فیصلے کرنے کے لیے استعمال کیا جا سکتا ہے۔

## بہترین طریقے اور عام غلطیاں
ٹریٹ پیش گوئی سسٹم بناتے وقت غور کرنے کے لیے کچھ بہترین طریقے اور عام غلطیاں یہ ہیں:

* **ہائی کوالٹی ڈیٹا استعمال کریں**: ڈیٹا کی کوالٹی ٹریٹ پیش گوئی سسٹم بنانے کے لیے اہم ہے۔
* **موزوں ML ماڈلز استعمال کریں**: ML ماڈل کا انتخاب مخصوص مسئلے اور ڈیٹا پر منحصر ہے۔
* **اوور فٹنگ سے بچیں**: اوور فٹنگ اس وقت ہوتی ہے جب ماڈل بہت پیچیدہ ہوتا ہے اور ڈیٹا میں شور سے فٹ ہوتا ہے۔
* **کراس ویلیڈیشن استعمال کریں**: کراس ویلیڈیشن ماڈل کی کارکردگی کی تشخیص کے لیے ضروری ہے۔

## ہینڈز آن مثال یا مینی پروجیکٹ
آئیے چاول ڈیٹاسیٹ کا استعمال کرتے ہوئے ٹریٹ پیش گوئی سسٹم بنائیں۔

```python
import pandas as pd
from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split

# چاول ڈیٹاسیٹ لوڈ کریں
rice_data = pd.read_csv('rice_data.csv')

# ڈیٹا کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں
X_train, X_test, y_train, y_test = train_test_split(rice_data, rice_phenotypic_data, test_size=0.2, random_state=42)

# رینڈم فارسٹ ماڈل ٹرین کریں
rf_model = RandomForestRegressor(n_estimators=100, random_state=42)
rf_model.fit(X_train, y_train)

# ٹیسٹنگ سیٹ پر پیش گوئیاں کریں
y_pred = rf_model.predict(X_test)

print(y_pred)
```

## خلاصہ ٹیبل یا چیک لسٹ
ٹریٹ پیش گوئی سسٹم بنانے کے لیے خلاصہ ٹیبل یا چیک لسٹ یہاں ہے:

| مرحلہ | تفصیل |
| --- | --- |
| 1 | ڈیٹا لوڈ اور پری پروسیس کریں |
| 2 | ڈیٹا کو ٹریننگ اور ٹیسٹنگ سیٹس میں تقسیم کریں |
| 3 | ML ماڈل ٹرین کریں |
| 4 | ٹیسٹنگ سیٹ پر پیش گوئیاں کریں |
| 5 | ماڈل کی کارکردگی کی تشخیص کریں |
| 6 | کیوزل ویریئنٹس کی شناخت کریں |
| 7 | بریڈنگ پروگرامز کے ساتھ انٹیگریٹ کریں |

## اگلے اقدامات اور مزید مطالعہ
مزید مطالعہ کے لیے، ہم درج ذیل ریسورسز کی سفارش کرتے ہیں:

* **پودوں کی بائیوٹیکنالوجی**: "Plant Biotechnology" by P. C. S. Krishna
* **مشین لرننگ**: "Machine Learning" by Andrew Ng
* **جینومکس**: "Genomics" by T. A. Brown

اگلے اقدامات:

* **ٹریٹ پیش گوئی سسٹم کو دیگر پودوں پر لاگو کریں**: سسٹم کو گندم، مکئی، یا سویا بینز جیسے دیگر پودوں پر لاگو کریں۔
* **دیگر ڈیٹا سورسز کے ساتھ انٹیگریٹ کریں**: سسٹم کو موسم، مٹی، یا سینسر ڈیٹا جیسے دیگر ڈیٹا سورسز کے ساتھ انٹیگریٹ کریں۔
* **دیگر ML ماڈلز استعمال کریں**: سسٹم کی درستگی کو بہتر بنانے کے لیے ڈیپ لرننگ یا گریدیئنٹ بوسٹنگ جیسے دیگر ML ماڈلز استعمال کریں۔ 💡

ہمیشہ ہائی کوالٹی ڈیٹا اور موزوں ML ماڈلز استعمال کریں، اور اوور فٹنگ اور عام غلطیوں سے بچیں۔ ⚠️ ان بہترین طریقوں پر عمل کرتے ہوئے اور ٹریٹ پیش گوئی سسٹم کا استعمال کرتے ہوئے، آپ فصل کی سلیکشن اور بریڈنگ کے بارے میں آگاہ فیصلے کر سکتے ہیں اور فصل کی پیداوار اور کوالٹی کو بہتر بنا سکتے ہیں۔ 🌱