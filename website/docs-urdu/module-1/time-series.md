---
sidebar_position: 5
---

# فصلوں کی نگرانی کے لیے ٹائم سیریز تجزیہ

## تعارف

پودوں کی بائیو ٹیکنالوجی کے میدان میں، پودوں کی نشوونما کے پیٹرن کو سمجھنا اور پیش گوئی کرنا فصلوں کی پیداوار کو زیادہ سے زیادہ کرنے اور فضلہ کو کم کرنے کے لیے بہت اہم ہے۔ اس کو حاصل کرنے کا ایک طاقتور ٹول ٹائم سیریز تجزیہ ہے، جس میں رجحانات، پیٹرن اور بے ضابطگیوں کی نشاندہی کرنے کے لیے وقت کے ساتھ بدلنے والے ڈیٹا کا تجزیہ شامل ہے 🌱۔

## بنیادی تصورات

* **سٹیشنریٹی (Stationarity)**: ایک ٹائم سیریز کو سٹیشنری کہا جاتا ہے اگر اس کی شماریاتی خصوصیات وقت کے ساتھ مستقل رہیں۔
* **ڈفرنسنگ (Differencing)**: غیر سٹیشنری ٹائم سیریز کو سٹیشنری بنانے کے لیے۔
* **آٹو کوریلیشن (Autocorrelation)**: ٹائم سیریز اور اس کے تاخیر شدہ ورژن کے درمیان ارتباط۔

### ARIMA ماڈلز

```python
import pandas as pd
from statsmodels.tsa.arima.model import ARIMA

# ڈیٹاسیٹ لوڈ کریں
df = pd.read_csv('wheat_yield.csv', index_col='date', parse_dates=['date'])

# ARIMA ماڈل بنائیں
model = ARIMA(df['yield'], order=(1,1,1))
model_fit = model.fit()
print(model_fit.summary())
```

### Facebook Prophet

```python
from prophet import Prophet

df = df.rename(columns={'date': 'ds', 'growth_rate': 'y'})
model = Prophet(yearly_seasonality=True)
model.fit(df)
forecast = model.predict(model.make_future_dataframe(periods=30))
```

## خلاصہ

| تکنیک | استعمال | فوائد |
|-------|--------|-------|
| ARIMA | روایتی ٹائم سیریز | آسان، تیز |
| Prophet | موسمی ڈیٹا | چھٹیوں کو سنبھالتا ہے |
| LSTM | پیچیدہ پیٹرن | غیر لکیری تعلقات |
