---
sidebar_position: 1
---

# زراعت میں کمپیوٹر وژن کا تعارف

## تعارف

کمپیوٹر وژن زراعت میں انقلاب لا رہا ہے، خودکار پودوں کی نگرانی، بیماریوں کا پتہ لگانے، اور فینوٹائپنگ کے ذریعے۔ یہ ماڈیول آپ کو زرعی امیج پروسیسنگ کی بنیاد سکھائے گا 🌿۔

## کمپیوٹر وژن کیا ہے؟

کمپیوٹر وژن مصنوعی ذہانت کا ایک شعبہ ہے جو کمپیوٹرز کو تصاویر اور ویڈیوز سے معنی خیز معلومات نکالنے کی اجازت دیتا ہے۔

### زراعت میں استعمال

| استعمال | تفصیل |
|--------|-------|
| بیماری کا پتہ لگانا | پتوں پر بیماری کی علامات |
| پھلوں کی گنتی | پیداوار کا اندازہ |
| فصل کی صحت | NDVI تجزیہ |
| گھاس پھوس کا پتہ | خودکار شناخت |

## تصویر کی بنیادیں

### رنگ کی جگہیں (Color Spaces)

```python
import cv2
import numpy as np

# تصویر پڑھیں
img = cv2.imread('plant_leaf.jpg')

# BGR سے RGB میں تبدیل کریں
rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)

# HSV میں تبدیل کریں (پودوں کے لیے بہتر)
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)

# سبز رنگ کا ماسک بنائیں
lower_green = np.array([35, 40, 40])
upper_green = np.array([85, 255, 255])
mask = cv2.inRange(hsv, lower_green, upper_green)

# ماسک لگائیں
result = cv2.bitwise_and(img, img, mask=mask)
```

### تصویری آلات

زرعی تصویری نظام میں شامل ہیں:
- **RGB کیمرے**: عام تصاویر
- **ملٹی سپیکٹرل کیمرے**: پودوں کی صحت کے لیے
- **تھرمل کیمرے**: پانی کے تناؤ کے لیے
- **ڈرون**: بڑے علاقوں کی تصویر کشی

## OpenCV کا تعارف

```python
import cv2
import matplotlib.pyplot as plt

# تصویر لوڈ کریں
img = cv2.imread('crop_field.jpg')

# سائز تبدیل کریں
resized = cv2.resize(img, (640, 480))

# گرے اسکیل میں تبدیل کریں
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

# کناروں کا پتہ لگائیں
edges = cv2.Canny(gray, 100, 200)

# نتائج دکھائیں
plt.subplot(121), plt.imshow(gray, cmap='gray')
plt.title('گرے اسکیل')
plt.subplot(122), plt.imshow(edges, cmap='gray')
plt.title('کنارے')
plt.show()
```

## عملی مثال: پتے کی تقسیم

```python
import cv2
import numpy as np

def segment_leaf(image_path):
    # تصویر پڑھیں
    img = cv2.imread(image_path)
    
    # HSV میں تبدیل کریں
    hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)
    
    # سبز رنگ کا ماسک
    mask = cv2.inRange(hsv, 
                       np.array([25, 40, 40]), 
                       np.array([95, 255, 255]))
    
    # شور ہٹائیں
    kernel = np.ones((5,5), np.uint8)
    mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel)
    mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, kernel)
    
    # نتیجہ
    result = cv2.bitwise_and(img, img, mask=mask)
    return result, mask

# استعمال
segmented, mask = segment_leaf('leaf.jpg')
```

## خلاصہ

- کمپیوٹر وژن زراعت کو خودکار بنا رہا ہے
- رنگ کی جگہیں (HSV, LAB) پودوں کے تجزیے کے لیے اہم ہیں
- OpenCV بنیادی لائبریری ہے

## اگلے اقدامات

- [امیج پروسیسنگ](/docs/module-2/image-processing) - تصویری پری پروسیسنگ سیکھیں
